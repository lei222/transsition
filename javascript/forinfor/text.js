for (let gun = 1; gun < 32; gun++) {
    for (let saat = 0; saat < 24; saat++) {
        for (let deq = 0; deq < 60; deq++) {
            for (let san = 0; san < 60; san++) {
                console.log(` ${gun} mart, ${saat} : ${deq} : ${san}`);
               
            }
            
            
        }
    }
}