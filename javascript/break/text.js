const adlar = ['ramo', 'leyla', 'nurlan', 'davud', 'anar', 'mitri'];
for (let i = 0; i < adlar.length; i++) {
    if (adlar[i] == 'anar') {
        continue;

    } else {
        console.log(adlar[i]);
    }
}
