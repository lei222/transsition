let adlar = ['elnure', 'leyla ', 'efsane', 'davud', 'nurlan', 'ayxan', 'vusale', 'elmaddin'];
for (let i = 0; i < adlar.length; i++) {
    if (adlar[i].length < 6) {
        console.log(adlar[i]);
    }
}